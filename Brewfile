# Installed by `just tools`. SwiftFormat is pinned through the Mintfile instead,
# because the formatter's version decides what counts as formatted and Homebrew
# publishes no versioned formula for it.
brew 'git-format-staged'
brew 'mint'
